@model ClassLibrary.Models.UnitSelectionViewModel
@{
    ViewBag.Title = "EditUser";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-3.5.1.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<script>
    $(document).ready(function () {
        $("#myModal").modal('show');
    });
</script>
<div id="myModal" class="modal fade">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h class="modal-title">Select Unit</h>


                <button type="button" class="close" data-dismiss="modal">&times;</button>

            </div>
            <div class="modal-body">

                            <div class="row">
                                <div class="col-sm-12">
                                    <div class="form-group row">
                                        <label class="col-sm-2 col-form-label">Select Unit</label>
                                        <div class="col-sm-6">
                                            @Html.DropDownListFor(model => model.tbl_Unit.Id, new SelectList(Model._tbl_Unit, "Id", "Name"), "Select", new { @class = "form-control", @required = "true" })
                                            @Html.ValidationMessageFor(model => model.tbl_Unit.Id, "", new { @class = "text-danger" })
                                        </div>
                                        <div class="col-sm-4">
                                            <input type="button" id="Proceed" onclick="goToDashboard()" value="Proceed" class="btn btn-primary btn-sm" />
                                        </div>                          
                                    </div>
                                </div>
                            </div>                   
                    </div>
                </div>

                <!-- /.row (main row) -->

            </div>
        </div>
<script>


    function goToDashboard() {
        var siteId = $('#tbl_Unit_Id').val();
        $.ajax({
            cache: 'false',
            type: "POST",
            url: '@Url.Action("Dashboard", "Admin")',
            dataType: 'json',
            data: {
                siteId: siteId,
            },
            success: function (response) {
                alert("success");
                location.href = "/Admin/Dashbard";
             },
            failure: function (response) {
                alert("fails");
                //var finalError = ''
                //for (i = 0; i < vm.responseJSON.length; i++) {
                //    finalError += vm.responseJSON[i].key + " : " + vm.responseJSON[i].errors[0] + "\n";
                //}

                //swal({ title: "ERROR", text: finalError, type: "error", });
            },
            error: function (response) {
                alert("fails");
                //console.log(response);

                //var finalError = ''
                //for (i = 0; i < vm.responseJSON.length; i++)
                //{
                //    finalError += vm.responseJSON[i].key + " : " + vm.responseJSON[i].errors[0] + "\n";
                //}

                //swal({ title: "ERROR", text: finalError, type: "error", });

            }
        });
    };
</script>